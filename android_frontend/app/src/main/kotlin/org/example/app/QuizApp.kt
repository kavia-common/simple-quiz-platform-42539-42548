package org.example.app

import android.app.Application

/**
 * Application class placeholder (future extension point).
 * Not strictly needed now but reserved for potential state scoping.
 */
class QuizApp : Application()
